<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CustomSpinner" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CustomSpinner" parent="Base.Theme.CustomSpinner" />

    <style name="CustomAutoCompleteTextViewStyle">
        <item name="android:popupBackground">@drawable/round_shape</item>
    </style>
</resources>